#include<stdio.h>
#include<math.h>
int main()
{
	 int n ;
	 printf("Enter the number to check");
	 scanf("%d",&n);
	 if(n%2== 0)
     {
         printf("the n is even");
     }
     else
     {
         printf("The no. is odd");
     }
	 return 0 ;
}
